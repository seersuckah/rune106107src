//=============================================================================
// RuneConsoleClientWindow -> Client are of Console Window
//=============================================================================
class RuneConsoleClientWindow extends UWindowConsoleClientWindow;

//#exec TEXTURE IMPORT NAME=RuneConsoleBackground FILE=Textures\Console3.pcx MIPS=OFF


//=============================================================================
// Superclass Function Overrides
//=============================================================================


/*
function Created()
{
	Super.Created();

}

function BeforePaint(Canvas C, float X, float Y)
{
	local	Color	col;

	col.R=255;
	col.G=0;
	col.B=0;

	Super.BeforePaint(C, X, Y);
	//TextArea.SetFont(F_RuneBig);
	TextArea.SetSize(WinWidth, WinHeight - EditControl.WinHeight);
	//TextArea.SetTextColor(col);
}



function Paint(Canvas C, float X, float Y)
{
	Super.Paint(C,X,Y);
	//Tile(C,Texture'RuneConsoleBackground');
}
*/

defaultproperties
{
}
